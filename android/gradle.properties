# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Optimized memory settings for AGP 8.8.0 and Android SDK 34
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+HeapDumpOnOutOfMemoryError -XX:+UseG1GC -XX:+UseStringDeduplication

# Build performance optimizations
# Parallel builds can improve build performance for multi-module projects
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.caching=true
org.gradle.workers.max=4

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Suppress deprecation warnings for Gradle compatibility
android.suppressUnsupportedCompileSdk=true

# Disable configuration cache - causes issues in CI/CD
org.gradle.configuration-cache=false
android.enableJetifier=true

# Suppress Gradle 9.0 compatibility warnings
android.suppressUnsupportedOptionWarnings=true
org.gradle.warning.mode=none
